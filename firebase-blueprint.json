{
  "entities": {
    "app_state": {
      "title": "AppState",
      "description": "Global shared collaborative state for projects, tasks, members, columns, comments, sprints and users",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "users": { "type": "array" },
        "projects": { "type": "array" },
        "members": { "type": "array" },
        "columns": { "type": "array" },
        "tasks": { "type": "array" },
        "sprints": { "type": "array" },
        "comments": { "type": "array" },
        "activityLogs": { "type": "array" },
        "attachments": { "type": "array" },
        "updatedAt": { "type": "string" },
        "updatedBy": { "type": "string" }
      },
      "required": ["id", "updatedAt"]
    }
  },
  "firestoreRules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /{document=**} {\n      allow read, write: if true;\n    }\n  }\n}"
}
